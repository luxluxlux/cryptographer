/**
 * The module containing components and utilities for building application pages.
 * @module
 */

export { default as Page } from './Page';
// TODO: Most likely it is necessary to move one component out of the module of another
export { default as Header } from './Header';

export type { IPageProps, IHeaderProps } from './interfaces';